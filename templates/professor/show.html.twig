{% extends 'base.html.twig' %}

{% block title %}Professor{% endblock %}

{% block body %}
    <h1>Professor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ professor.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ professor.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ professor.roles ? professor.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ professor.password }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ professor.name }}</td>
            </tr>
            <tr>
                <th>IsVerified</th>
                <td>{{ professor.isVerified ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>PasswordChangeDate</th>
                <td>{{ professor.passwordChangeDate ? professor.passwordChangeDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_professor_index') }}">back to list</a>

    <a href="{{ path('app_professor_edit', {'id': professor.id}) }}">edit</a>

    {{ include('professor/_delete_form.html.twig') }}
{% endblock %}
